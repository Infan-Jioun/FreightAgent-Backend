enum Role {
    ADMIN
    AGENT
    CUSTOMER
}

enum ShipmentStatus {
    PENDING       
    ASSIGNED      
    ACCEPTED    
    PICKED_UP     
    IN_TRANSIT    
    AT_CUSTOMS
    OUT_FOR_DELIVERY
    DELIVERED     
    CANCELLED     
}

enum LocationType {
    SEA_PORT
    AIR_PORT
    INLAND_CONTAINER_DEPOT
}

enum CredentialType {
    FIATA_MEMBERSHIP
    IATA_ACCREDITATION
    NVOCC_LICENSE
    AEO_CERTIFICATE
    ISO_9001
    LOCAL_TRADE_LICENSE
    LOCAL_CUSTOMS_LICENSE
}

enum CredentialStatus {
    PENDING
    VERIFIED
    REJECTED
    EXPIRED
}

enum AgentVerificationStatus {
    NOT_APPLICABLE
    PENDING
    VERIFIED
    REJECTED
    SUSPENDED
}

enum PaymentStatus {
    UNPAID
    PROCESSING
    PAID
    FAILED
    REFUNDED
}

enum AuditAction {
    VERIFY_CREDENTIAL
    REJECT_CREDENTIAL
    APPROVE_AGENT
    REJECT_AGENT
    SUSPEND_AGENT
    CREATE_LOCATION
    UPDATE_LOCATION
    BLOCK_LOCATION
    DELETE_LOCATION
    ASSIGN_AGENT
    PROCESS_REFUND
}